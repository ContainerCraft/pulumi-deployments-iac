config:
  kubernetes-platform:cert_manager.enabled: "false"
  kubernetes-platform:civo_kubernetes.deploy: "true"
  kubernetes-platform:pulumi_cloud.deployment: "true"
  kubernetes-platform:pulumi_cloud.schedule: "true"
environment:
  - civo
